# Medium Profiles

This directory contains the profile adaptation layers for different device categories (e.g. workstations, smart home appliances, mobile devices). These adaptation layers translate device-specific hardware capability parameters into uniform system-wide capabilities specified in `proto/topology.proto`.
